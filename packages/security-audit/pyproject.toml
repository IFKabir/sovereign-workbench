[project]
name = "security-audit"
version = "0.1.0"
description = "SHA-256 hash-chained tamper-evident audit ledger"
requires-python = ">=3.11"
dependencies = ["shared-schemas", "python-jose", "fastapi"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
